package javaCalendar;

/** JEventIO Class
 * Assigned to: Charles (cws2017)
 * Subclass to CHandler
 * Handles the file opening and closing operations for CHandler.
 *
 * Functions:
 * boolean open(String): Reads events from the file at the given filepath. Returns whether the action was successful
 * boolean save(String): Saves events to the file at the given path. Returns whether the action was successful
 *
 * Javadocs created by dandreas on 4/4/17.
 */

public class JEventIO
{

}
